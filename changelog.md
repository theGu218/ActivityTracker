\# 📜 开发者更新日志 / 项目历程回顾



\*\*项目名称：ActivityTracker - 电脑时间去向监控系统\*\*

\*\*核心目标：精准记录电脑各软件的前台、最小化、隐藏托盘时长，并提供多维度的数据展示。\*\*



\---



\## v0.1 - 初始萌芽 (单文件打天下)

\- \*\*功能\*\*：利用 `psutil` 和 `win32gui`，实现每10秒抓取一次前台窗口，并将程序名和累计秒数存入 JSON 文件。

\- \*\*局限\*\*：只能记录用户“正在看”的软件，后台程序（网易云、木木模拟器）完全无法统计。

\- \*\*原始数据格式\*\*：`{ "pycharm64.exe": 3600 }` (纯数字时长)。



\## v0.2 - 引入可视化 (Streamlit 网页版诞生)

\- \*\*新增\*\*：加入 `Streamlit` 网页看板，让枯燥的 JSON 数据变成了实时的排行榜和图表。

\- \*\*重大认知更新\*\*：发现后台挂机程序（网易云听歌、木木挂机）不能被记录，这违背了“统计电脑总使用时长”的初衷。



\## v0.3 - 开启后台革命 (白名单扫描与时间均摊)

\- \*\*新增后台扫描\*\*：每30秒扫描一次系统进程，如果发现白名单内的程序（`cloudmusic`, `mumu` 等），就为其累加时间。

\- \*\*🚨 致命漏洞（时间膨胀）\*\*：发现同时挂机 2 个程序时，30秒内会给两者各加 30 秒，导致总时长超过真实物理时间，出现“一天累计 20 小时”的假数据。

\- \*\*✅ 修复\*\*：引入 \*\*“并发均摊”\*\* 算法。扫描到 N 个程序同时运行，将 30 秒物理时间平均分给它们，彻底解决了数据失真问题。



\## v0.4 - 精细化判定 (CPU活跃度与兜底机制)

\- \*\*引入 CPU 检测\*\*：为了防止“挂机但没干活”的时间被虚假记录（比如网易云挂后台但不放歌），加入了 `cpu\_percent < 0.5` 则不计时的过滤机制。

\- \*\*强制白名单\*\*：发现现代音乐播放器使用显卡解码，CPU占用极低（接近0%）。为防止漏记，为 `cloudmusic.exe` 加入了 \*\*强制白名单\*\*（无视 CPU 占用，直接记录）。



\## v0.5 - 多维结构化升级 (区分状态与时间维度)

\- \*\*数据结构重构\*\*：从单一的 `{应用: 秒数}` 升级为按日期分组的 `{日期: {应用: {fg, bg, hidden}}}`。

\- \*\*状态拆分\*\*：后台应用被细分为 \*\*🟢 前台\*\*、\*\*🟡 最小化/遮挡\*\*、\*\*🔴 纯隐藏托盘\*\* 三类，展示更精确。

\- \*\*时间维度\*\*：看板支持切换查看 \*\*📅 今天\*\*、\*\*📆 本周\*\* 和 \*\*📊 累计至今\*\* 的数据。



\## v0.6 - 界面现代化与打包化 (tkinter vs Python 3.14 终极大战)

\- \*\*初步美化\*\*：起初用原生的 `tkinter`，界面非常复古（90年代风格）。

\- \*\*引入现代 UI\*\*：尝试用 `ttkbootstrap` 换肤，瞬间拥有极简扁平化风格（`cosmo` 主题）。

\- \*\*🚨 环境大战\*\*：Python 3.14 的测试版与 `ttkbootstrap` 出现严重的 `ImportError` 兼容性冲突。最终为了确保软件在现有 Python 3.14 环境下的稳定性，回退到了原生的 `tkinter` 搭配 `clam` 主题。

\- \*\*核心功能：“一触即达”自唤醒机制\*\*：在 `main.py` 中加入子进程守护逻辑，双击 `.exe` 时自动在后台无弹窗唤醒 `src/tracker.py`，解决了“必须先手动开追踪器”的繁琐痛点。

\- \*\*独立打包\*\*：使用 `pyinstaller` 成功打包成独立的 `.exe` 文件。



\## v0.7 - 终极形态 (开机自启与闭环)

\- \*\*加入 【开机自动启动】 开关\*\*：在设置页面一键启用，自动在 Windows 启动目录创建带 `--service` 参数的快捷方式。

\- \*\*体验闭环\*\*：电脑开机后，程序将自动在后台静默记账，用户只需双击 `.exe` 即可弹出界面查看详细数据，全程无需任何额外操作。



\---



\### 💡 踩过的“深坑”总结：

1\. \*\*后台并发均摊\*\*：必须保证物理时间的真实性，不能让后台程序“分身”导致总时间超标。

2\. \*\*CPU与显卡硬解的冲突\*\*：如何让一个几乎不占 CPU 的音乐播放器（网易云）在后台依然能被可靠记录。

3\. \*\*Python 3.14 vs 第三方 UI 库\*\*：为了现代化界面，经历了漫长痛苦的环境兼容性排查，最终为了稳定性做出了权衡。

4\. \*\*隐藏窗口与无感启动\*\*：如何实现“双击主程序，后台服务同时静默苏醒”的无缝体验，尝试了各种 `.bat` 和 `.vbs` 骚操作，最后通过 `subprocess` + `pythonw.exe` 在代码层彻底解决。



\---

\*\*开发者日志完\*\*

